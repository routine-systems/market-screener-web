#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
if [[ $# -ne 1 ]]; then
  echo "usage: $0 /path/to/immutable-signals-bundle.v1.json" >&2
  exit 64
fi
BUNDLE="$1"
python3 "$ROOT/render_site.py" --bundle "$BUNDLE" --output "$ROOT/dist"
